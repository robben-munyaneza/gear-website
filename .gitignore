# Don't push large backup zips
*.zip

# Don't push temp restore folders
_restore_temp/

# Don't push server error logs
error_log

# Ignore stray .htaccess files, but keep the root one (SPA rewrite rules)
.htaccess
!/.htaccess
